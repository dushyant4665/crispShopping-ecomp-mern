{
  "version": 2,
  "builds": [
    { "src": "client/package.json", "use": "@vercel/static-build" },
    { "src": "server/dbserver.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/dbserver.js" },
    { "src": "/(.*)", "dest": "client/build/$1" }
  ]
}
